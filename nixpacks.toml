# Nixpacks 配置 - 让 Railway 自动检测 Python
[phases.setup]
nixPkgs = ["python311Full"]

[phases.install]
cmds = ["pip3 install -r backend/requirements.txt"]

[start]
cmd = "cd backend && python3 -m uvicorn app.main:app --host 0.0.0.0 --port ${PORT:-8000}"
